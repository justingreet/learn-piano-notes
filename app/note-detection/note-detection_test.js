'use strict';

describe('pianoPitchDetector.note-detection module', function() {

  beforeEach(module('pianoPitchDetector.note-detection'));

  describe('note-detection controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('NoteDetectionController');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});